global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: llm_gateway
    metrics_path: /metrics
    static_configs:
      - targets:
          - enterprise-ai-platform:8000
