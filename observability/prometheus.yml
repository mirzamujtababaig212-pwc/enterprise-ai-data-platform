global:

  scrape_interval: 15s

rule_files:

  - alerts.yml

scrape_configs:

  - job_name: llm-gateway

    metrics_path: /metrics

    static_configs:

      - targets:
          - host.docker.internal:8000
